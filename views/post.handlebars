        <div class="row mb-4 post">
          <div class="col-md-5">
            <h2>{{post.title}}</h2>
            <p>Created by
              {{post.user_name}}
              on
              {{format_date post.date_created}}</p>
          </div>
          <div class="col-md-7">
            <p>
              {{post.content}}
            </p>
          </div>
            {{#if comments.length}}
    <div class="col-md-6">
      <h3>Comments:</h3>

    {{#each comments as |comment|}}
    <div class="row mb-4">
        <div class="col-md-5">
            <h2>Created by{{comment.user_name}}</h2>
            <div class="col-md-7">
                <p>
                {{comment.content}}
                </p>
            </div>
    </div>
    {{/each}}
    </div>
    {{/if}}
    </div>
</div>